/**
 * PTM v2 - API Base Configuration
 *
 * @author QuanTuanHuy
 * @description Part of Serp Project - RTK Query setup using shared API
 */

import { api } from '@/lib/store/api';

// PTM v2 uses the shared API instance from lib/store/api
// Tag types are registered in the shared API configuration
export const ptmApi = api;
