/**
 * Author: QuanTuanHuy
 * Description: Part of Serp Project - Profile layout wrapper
 */

import { ProfileLayout } from '@/modules/account';

export default function Layout({ children }: { children: React.ReactNode }) {
  return <ProfileLayout>{children}</ProfileLayout>;
}
